<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foreach.imageserver.core.data.WebImageParametersDao">
    <sql id="columns">
		WIP.imageId as web_image_parameters_imageId,
        WIP.url as web_image_parameters_url,
        WIP.width as web_image_parameters_width,
        WIP.height as web_image_parameters_height,
        WIP.imageTypeId as web_image_parameters_imagetype
	</sql>

    <resultMap id="webImageParametersMap" type="WebImageParameters">
        <id property="imageId" column="web_image_parameters_imageId"/>
        <result property="url" column="web_image_parameters_url"/>
        <result property="dimensions.width" column="web_image_parameters_width"/>
        <result property="dimensions.height" column="web_image_parameters_height"/>
        <result property="imageType" column="web_image_parameters_imagetype"
                typeHandler="com.foreach.imageserver.core.data.handlers.ImageTypeHandler"/>
    </resultMap>

    <select id="getById" parameterType="int" resultMap="webImageParametersMap">
        SELECT
        <include refid="columns"/>
        FROM WEB_IMAGE_PARAMETERS WIP
        WHERE WIP.imageId = #{value}
    </select>

    <select id="getByParameters" parameterType="String" resultMap="webImageParametersMap">
        SELECT
        <include refid="columns"/>
        FROM WEB_IMAGE_PARAMETERS WIP
        WHERE WIP.url = #{value}
    </select>

    <insert id="insert" parameterType="WebImageParameters">
        INSERT INTO WEB_IMAGE_PARAMETERS (
            imageId,
            url,
            width,
            height,
            imageTypeId
        ) VALUES (
            #{imageId},
            #{url},
            #{dimensions.width},
            #{dimensions.height},
            #{imageType.id}
        )
    </insert>
</mapper>

