<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foreach.imageserver.core.data.ImageDao">

    <sql id="columns">
		I.imageId as image_id,
        I.applicationId as image_applicationid,
        I.created as image_datecreated,
        I.repositoryCode as image_repository_code,
        I.originalImageId as image_original_image_id
	</sql>

    <resultMap id="imageMap" type="Image">
        <id property="imageId" column="image_id"/>
        <id property="applicationId" column="image_applicationid"/>
        <result property="dateCreated" column="image_datecreated"/>
        <result property="repositoryCode" column="image_repository_code"/>
        <result property="originalImageId" column="image_original_image_id"/>
    </resultMap>

    <select id="getById" parameterType="map" resultMap="imageMap">
        SELECT
        <include refid="columns"/>
        FROM Image I
        WHERE I.imageId = #{imageId} AND I.applicationId = #{applicationId}
    </select>

    <insert id="insert" parameterType="Image">
        INSERT INTO Image (
            imageId,
            applicationId,
            repositoryCode,
            originalImageId
        ) VALUES (
            #{imageId},
            #{applicationId},
            #{repositoryCode},
            #{originalImageId}
        )
    </insert>

</mapper>