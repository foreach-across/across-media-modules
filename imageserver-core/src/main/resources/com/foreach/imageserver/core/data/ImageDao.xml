<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.foreach.imageserver.core.data.ImageDao">

    <sql id="columns">
		I.id as image_id,
		I.externalId as image_external_id,
        I.created as image_datecreated,
        I.width as image_width,
        I.height as image_height,
        I.imageTypeId as image_imagetype
	</sql>

    <resultMap id="imageMap" type="Image">
        <id property="id" column="image_id"/>
        <result property="externalId" column="image_external_id"/>
        <result property="dateCreated" column="image_datecreated"/>
        <result property="dimensions.width" column="image_width"/>
        <result property="dimensions.height" column="image_height"/>
        <result property="imageType" column="image_imagetype"
                typeHandler="com.foreach.imageserver.core.data.handlers.ImageTypeHandler"/>
    </resultMap>

    <select id="getById" parameterType="map" resultMap="imageMap">
        SELECT
        <include refid="columns"/>
        FROM Image I
        WHERE I.id = #{id}
    </select>

    <select id="getByExternalId" parameterType="map" resultMap="imageMap">
        SELECT
        <include refid="columns"/>
        FROM Image I
        WHERE I.externalId = #{externalId}
    </select>

    <insert id="insert" parameterType="Image">
        <selectKey keyProperty="id" resultType="int" order="BEFORE">
            SELECT SEQ_IMAGES.nextval AS ID FROM DUAL
        </selectKey>
        INSERT INTO Image (
        id,
        externalId,
        created,
        width,
        height,
        imageTypeId
        ) VALUES (
        #{id},
        #{externalId},
        #{dateCreated},
        #{dimensions.width,jdbcType=INTEGER},
        #{dimensions.height,jdbcType=INTEGER},
        #{imageType.id,jdbcType=INTEGER}
        )
    </insert>

</mapper>