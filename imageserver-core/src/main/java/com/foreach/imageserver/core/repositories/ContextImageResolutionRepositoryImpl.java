package com.foreach.imageserver.core.repositories;

import com.foreach.imageserver.core.business.ContextImageResolution;
import org.springframework.stereotype.Repository;

@Repository
public class ContextImageResolutionRepositoryImpl extends BasicRepositoryImpl<ContextImageResolution> implements ContextImageResolutionRepository
{
}
