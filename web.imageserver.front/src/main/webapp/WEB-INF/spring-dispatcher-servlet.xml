<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
           http://www.springframework.org/schema/mvc
           http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

	<bean class="org.springframework.context.support.PropertySourcesPlaceholderConfigurer">
		<property name="locations">
			<list>
				<value>classpath:/config/${environment.type}/common.properties</value>
				<value>file:${user.home}/dev-configs/imageserver.local.properties</value>
			</list>
		</property>
		<property name="ignoreUnresolvablePlaceholders" value="true"/>
		<property name="ignoreResourceNotFound" value="true"/>
	</bean>

	<bean class="com.foreach.imageserver.admin.utils.WebPathConfiguration">
		<property name="siteRoot" value="${path.siteRoot}"/>
		<property name="resources" value="${path.resources}"/>
	</bean>

	<import resource="classpath:config/spring-config-services.xml"/>

	<mvc:interceptors>
		<!-- Set global variables if all other interceptors have passed (they could modify settings first) -->
		<bean class="com.foreach.imageserver.admin.utils.GlobalVariableInterceptor"/>
		<!-- Disable client caching on all requests (to be finetuned) -->
		<bean class="org.springframework.web.servlet.mvc.WebContentInterceptor">
			<property name="cacheSeconds" value="0"/>
			<property name="useExpiresHeader" value="true"/>
			<property name="useCacheControlHeader" value="true"/>
			<property name="useCacheControlNoStore" value="true"/>
		</bean>
	</mvc:interceptors>

	<bean class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="viewClass" value="org.springframework.web.servlet.view.JstlView"/>
		<property name="prefix" value="/WEB-INF/jsp/"/>
		<property name="suffix" value=".jsp"/>
		<property name="redirectContextRelative" value="true"/>
		<property name="order" value="1"/>
	</bean>


	<context:annotation-config/>

	<context:component-scan base-package="com.foreach.imageserver"/>
</beans>