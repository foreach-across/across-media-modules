<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
           http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
           http://www.springframework.org/schema/context
           http://www.springframework.org/schema/context/spring-context.xsd">

	<context:annotation-config/>

	<bean id="propertyConfigurer"
          class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
        <property name="ignoreResourceNotFound" value="true"/>
        <property name="locations">
            <list>
	            <value>classpath:/config/variantlog.properties</value>
                <value>classpath:/config/${environment.type}/common.properties</value>
                <value>file:/config-override/imageserver/front.properties</value>
            </list>
        </property>
    </bean>

    <bean class="com.foreach.imageserver.admin.utils.WebPathConfiguration">
        <property name="siteRoot" value="${path.siteRoot}"/>
        <property name="resources" value="${path.resources}"/>
    </bean>

	<bean class="com.foreach.spring.logging.Log4JConfigurator" init-method="configure" lazy-init="false">
        <property name="propertiesFile" value="${log.properties}"/>
        <property name="logDirectory" value="${log.dir}"/>
    </bean>

	<import resource="classpath:config/spring-config-rendering.xml"/>
    <import resource="classpath:config/spring-config-services.xml"/>
    <import resource="classpath:config/spring-config-utils.xml"/>
    <import resource="spring-config-security.xml"/>

</beans>

