package com.foreach.imageserver;

import cucumber.annotation.en.And;
import cucumber.annotation.en.Given;
import cucumber.runtime.PendingException;

public class LoadImageSteps
{
	@Given("^the test environment is up and running$")
	public void the_test_environment_is_up_and_running() throws Throwable
	{
		// Express the Regexp above with the code you wish you had
		throw new PendingException();
	}

	@And("^there is a source image on \"([^\"]*)\"$")
	public void there_is_a_source_image_on( String imgUrl ) throws Throwable
	{
		// Express the Regexp above with the code you wish you had
		throw new PendingException();
	}
}
